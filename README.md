# Batch_Kerberos_Delegation
A Powershell script to enable kerberos delegation for a batch of objects within one or multiple active directory (AD) organization units (OUs) for Windows Admin Center management. 
